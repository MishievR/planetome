<div class="row">
  <div class="col-12 col-md-7 col-lg-8 col-xl-9">
    <h1 class="main-text-color"><%= @job.position %>
      <% if current_user && current_user.admin? %>
      | <%= link_to "(edit)", edit_job_path(@job) %>
      <% end %>
    <br>
    <small class="text-muted"> <small>at</small>  <%= link_to @job.employer.company_name, employer_path(@job.employer), class: "normal-link-light-grey text-muted" %></small>
  </h1>
    <p class="text-muted">
    <% @job_categories.each do |category| %>
    • <%= link_to category.title, category_path(category), class: "normal-link-red text-muted" %>
    <% end %>
    </p>
  </div>
    <div class="col-12  col-md-5 col-lg-4 col-xl-3">
      <%= link_to "Apply", "https://#{@job.link}", class: "btn btn-danger btn-block" %>
    </div>
</div>

<br>
<br>
<div class="row">
  <div class="col-12">
    <div class="card card-community-show job-show-impact">
      <div class="card-body">
        <h6><strong>Job impact:</strong></h6>
        <h4><%= @job.impact %></h4>
    </div>

    </div>
  </div>
</div>

<br>

<div class="row">
  <div class="col-12">
    <div class="card">
      <div class="card-body">
        <h6><strong>Job description:</strong></h6>
        <p><%= simple_format @job.job_description %></p>
        <br>
        <% if @job.responsibilities != "" %>
          <h6><strong>Responsibilities:</strong></h6>
          <p><%= simple_format @job.responsibilities %></p>
          <br>
        <% end %>
        <% if @job.requirements != "" %>
          <h6><strong>Requirements:</strong></h6>
          <p><%= simple_format @job.requirements %></p>
          <br>
        <% end %>
        <% if @job.is_special_requirements == true %>
          <% if @job.special_requirements != "" %>
            <div class="card">
              <div class="card-body">
                <h6><strong>Special requirements:</strong></h6>
                <p><%= simple_format @job.special_requirements %></p>
                <br>

              </div>
            </div>
            <% else %>
            <h6><strong>This job have special requirements.</strong></h6>
            <br>

          <% end %>
        <% end %>
        <hr>
        <% if @job.location != "" %>
          <h6><strong>Location:</strong></h6>
          <p><%= @job.location %>
        <% end %>
        <% if @job.is_remote == true %>
          | <strong>You can work remote.</strong>
          <% end %></p>
        <br>

        <% if @job.salary != nil %>
          <h6><strong>Salary:</strong></h6>
          <p>$<%= @job.salary %>K /year</p>
          <br>
        <% end %>
        <% if current_user %>
        <h6><strong>Email to contact:</strong></h6>
        <p><%= @job.email_to_contact %></p>
        <br>
        <% end %>

        <div class="row">
          <div class="col-12">
            <%= link_to "Apply", "https://#{@job.link}", class: "btn btn-outline-danger btn-lg btn-block" %>
          </div>
        </div>
      </div>
    </div>
  </div>
</div>
