<nav class="navbar navbar-expand-lg navbar-light navbar-margin navbar-color-shadow ">
  <%= link_to image_tag('logo.png', style: 'height:54px;width:auto;'), root_path %>
  <button class="navbar-toggler custom" type="button" data-toggle="collapse" data-target="#navbarText" aria-controls="navbarText" aria-expanded="false" aria-label="Toggle navigation">
    <span class="navbar-toggler-icon"><i class="fas fa-bars"></i></span>
  </button>
  <div class="collapse navbar-collapse" id="navbarText">
    <ul class="navbar-nav mr-auto">
      <li class="nav-item">
        <%= link_to "Discover", root_path, class: "nav-link"  %>
      </li>





      <li class="nav-item dropdown">
        <a class="nav-link " href="#" id="navbarDropdownMenuLink" role="button" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
          •••
        </a>
        <div class="dropdown-menu" aria-labelledby="navbarDropdownMenuLink">
          <!-- <p class="dropdown-header">This section currently under development.
            <br>
            Stay tuned for awesome features!</p> -->

          <%= link_to "Connect", users_path, class: "dropdown-item"  %>
          <%= link_to "Projects", projects_path, class: "dropdown-item"  %>

<% if current_user && current_user.admin? %>
          <%= link_to "Topics", topics_path, class: "dropdown-item"  %>
          <%= link_to "Communities", communities_path, class: "dropdown-item"  %>
          <%= link_to "Ideas", ideas_path, class: "dropdown-item"  %>
          <%= link_to "Fields", fields_path, class: "dropdown-item"  %>
          <%= link_to "Jobs", jobs_path, class: "dropdown-item"  %>
          <%= link_to "Places", places_path, class: "dropdown-item"  %>
          <%= link_to "Categories", categories_path, class: "dropdown-item"  %>
          <!-- <a class="dropdown-item" href="#">About</a> -->

          <% end %>
        </div>
      </li>


    </ul>
    <span class="navbar-text mr-2">
      <% if user_signed_in? %>
        <%= image_tag current_user.avatar.url(:medium), class: "profilePhotoNav" if current_user.avatar? %>
        <%= link_to(user_path(current_user), class: "btn btn-post-job") do %>
        <%= current_user.first_name %>
        <% if current_user.admin? %><% end %>
        <% end %>
      <% else %>
        <%= link_to(new_user_session_path, :class=>"btn") do %>
          <i class="fas fa-sign-in-alt"></i>
          Log In
        <% end %>
      <% end %>
    </span>
    <span class="navbar-text">
      <% if current_user.present? %>
      <%= link_to "Discover a topic", topics_path, data: { turbolinks: false }, class: "btn btn-danger btn-post-job good-font"%>
      <% else %>
      <%= link_to "Sign Up", new_user_registration_path, data: { turbolinks: false }, class: "btn btn-danger btn-post-job good-font"%>
      <% end %>
    </span>
  </div>

</nav>
