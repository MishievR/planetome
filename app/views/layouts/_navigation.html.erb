<nav class="navbar navbar-expand-lg navbar-light navbar-margin navbar-color-shadow ">
  <%= link_to image_tag('logo.png', style: 'height:30px;width:auto;margin-top:5px;'), root_path %>
  <!-- <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarNavDropdown" aria-controls="navbarNavDropdown" aria-expanded="false" aria-label="Toggle navigation">
    <span class="navbar-toggler-icon"></span>
  </button> -->
  <!-- <div class="collapse navbar-collapse" id="navbarNavDropdown"> -->
    <ul class="navbar-nav mr-auto">
    </ul>

    <ul class="navbar-nav">


      <button type="button" class="btn  btn-sm btn-link mr-2 btn-menu-nav pr-5 " data-toggle="modal" data-target="#exampleModal">
        <i class="far fa-compass"></i>
      </button>

      <div class="modal fade" id="exampleModal" tabindex="-1" role="dialog" aria-labelledby="exampleModalLabel" aria-hidden="true">
        <div class="modal-dialog modal-sm" role="document">
          <div class="modal-content model-dialog-menu">
            <div class="modal-header modal-header-menu">
              <h5 class="modal-title modal-title-menu" id="exampleModalLabel">Menu</h5>
              <button type="button" class="close" data-dismiss="modal" aria-label="Close">
                <span aria-hidden="true">&times;</span>
              </button>
            </div>
            <div class="modal-body">
              <% if user_signed_in? %>
              <div class="row">
                  <%= image_tag current_user.avatar.url(:thumb), class: "profilePhotoMenu" if current_user.avatar? %>
                  <% if current_user == @user && @user.first_name == nil %>
                  <h5 class="user-title-menu"><%= link_to "Add info", edit_user_path(current_user), class: "user-link-menu" %></h5>
                  <% else %>
                  <h5 class="user-title-menu"><%= link_to current_user.name, user_path(current_user), class: "user-link-menu" %></h5>
                  <% end %>

              </div>
              <%= link_to(communities_path, class: "dropdown-item dropdown-item-menu") do %>
              <i class="fab fa-buromobelexperte"></i>
              Communities
              <% end %>
              <%= link_to(fields_path, class: "dropdown-item dropdown-item-menu") do %>
              <i class="fab fa-buromobelexperte"></i>
              Fields
              <% end %>
              <%= link_to(cities_path, class: "dropdown-item dropdown-item-menu") do %>
              <i class="fab fa-buromobelexperte"></i>
              Cities
              <% end %>
              <div class="dropdown-divider"></div>
              <%= link_to(user_path(current_user), class: "dropdown-item dropdown-item-menu") do %>
              <i class="fas fa-user"></i>
              My profile
              <% end %>
              <%= link_to(edit_user_path(current_user), class: "dropdown-item dropdown-item-menu") do %>
              <i class="fas fa-edit"></i>
              Edit profile
              <% end %>
              <div class="dropdown-divider"></div>
              <%= link_to(destroy_user_session_path, method: :delete, class: "dropdown-item dropdown-item-menu") do %>
              <i class="fas fa-sign-out-alt"></i>
              Log out
              <% end %>

            <% else %>
            <%= link_to(fields_path, class: "dropdown-item dropdown-item-menu") do %>
            <i class="fab fa-buromobelexperte"></i>
            Fields
            <% end %>
            <%= link_to(cities_path, class: "dropdown-item dropdown-item-menu") do %>
            <i class="fab fa-buromobelexperte"></i>
            Cities
            <% end %>
            <%= link_to(new_user_session_path, :class=>"dropdown-item dropdown-item-menu") do %>
              <i class="fas fa-sign-in-alt"></i>
              Log In
            <% end %>


          <% end %>

        </div>
      </div>
    </div>
  </div>




<!--
      <div class="btn-group dropleft">
        <button class="btn btn-sm btn-link mr-2 btn-menu-nav pr-5 "  role="button" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
          <i class="far fa-compass"></i>
        </button>

        <div class="dropdown-menu dropdown-menu-right" aria-labelledby="dropdownMenuLink">
          <%= link_to "Log in", new_user_session_path, class: "dropdown-item" %>
          <div class="dropdown-divider"></div>
          <%= link_to "Log Out", destroy_user_session_path, method: :delete, class: "dropdown-item" %>
          <a class="dropdown-item" href="#">Action</a>
          <a class="dropdown-item" href="#">Another action</a>
          <a class="dropdown-item" href="#">Something else here</a>
        </div>
      </div> -->


    </ul>
  <!-- </div> -->
</nav>
