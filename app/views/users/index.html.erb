<!DOCTYPE html>
<html lang="en">
  <head>
    <title>Planetome Users List</title>
  </head>
<h1 align="center">All Planetome Users</h1>
<div align="center">
  <% @users.each do |user| %>
    <ul class="listing">
      <div class="container-fluid">
        <div class="row">
          <div class="well col-md-4 col-md-offset-4">
            <li><%= link_to gravatar_for(user), user_path(user) %></li>
            <li class="listing-title">
              <%= link_to user.name, user_path(user) %>
            </li>
            <li><small><%= pluralize(user.listings.count, "listing") if user.listings %></small></li>
            <% if user_signed_in? and current_user.admin? %>
              <li><%= link_to 'Delete this user', user_path(user), method: :delete,
              data: { confirm: "Are you sure you want to delete the user and all their listings?"} %></li>
              <li><%= link_to 'Edit this user', edit_user_path(user), method: :edit %></li>
            <% end %>
          </div>
        </div>
      </div>
    </ul>
  <% end %>
</div>
